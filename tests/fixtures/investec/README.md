# Investec Fixtures

The fixtures in this directory are synthetic contract-shape examples. They contain no real
credentials, account numbers, or client data and must not be described as live sandbox captures.

Replace or supplement them with sanitized sandbox responses during A1 verification. Remove or
mask authorization headers, access tokens, API keys, full account numbers, and personal data.

`balance-response.synthetic.json` documents the expected single-account balance envelope used by
the account and balance normalizer tests. It is synthetic until verified against the live sandbox.
